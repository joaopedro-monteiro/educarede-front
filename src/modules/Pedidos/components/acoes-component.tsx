import { User } from "../../Login/commands/user-command";
import { PedidosInterface } from "./pedidos";

function AcoesPedidos({key}: PedidosInterface, user: User) {
    let mensagem;

    if(key)
    {
        
    }
}

export default AcoesPedidos;